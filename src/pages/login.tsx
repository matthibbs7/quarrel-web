import React from 'react';
import { Flex, Text } from '@chakra-ui/react';

export default function Login() {
    return (
        <Flex>
            <Text mt={10} color='black' fontFamily='SFPRO'>Login</Text>
        </Flex>
    );
}
